{
  "name": "@eudi-wallet/issuer-sdk",
  "version": "0.1.0",
  "description": "Credential issuer SDK — OID4VCI server, SD-JWT VC + mDoc signing, revocation",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@eudi-wallet/core": "workspace:*",
    "jose": "^5.3.0",
    "cbor2": "^1.8.0",
    "@noble/hashes": "^1.4.0",
    "@noble/curves": "^1.4.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
